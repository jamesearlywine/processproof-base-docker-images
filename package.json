{
  "name": "processproof-base-lambda-images-for-lambdas",
  "version": "1.0.0",
  "description": "ProcessProof - Base Docker Images for Lambdas",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  }
}
